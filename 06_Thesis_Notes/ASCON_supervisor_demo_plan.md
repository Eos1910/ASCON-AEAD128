# ASCON Supervisor Demo Plan

Show:

1. ASCON_FYP folder structure.
2. Firmware interface: `k`, `n`, `p`, `r`.
3. Test-mode trace collection for byte 00 and byte 01.
4. Metadata table with `key_byte_value` labels.
5. Preliminary byte 00 training result.
6. Decision whether full 30k-per-byte ASCON collection is worthwhile.
