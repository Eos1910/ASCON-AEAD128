# full_ascon

This folder should contain the complete ASCON-AEAD-128 firmware.

Current files are scaffolds:

- `api.h`
- `encrypt.c`
- `main.c`
- `Makefile`

Replace `encrypt.c` with the real ASCON-AEAD-128 implementation before collecting thesis data.
